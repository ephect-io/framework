<?php

namespace Fun;

function App()
{
    return (<<<HTML
    <Router>
        <Route method="GET" rule='^/$' redirect={Home} />
        <Route method="GET" rule='^/info(/)?$' redirect={Info} />
    </Router>
    HTML);
}
