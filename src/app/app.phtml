<?php

namespace Fun;

use Home as SweetHome;

function App()
{
    return (<<<HTML
    <Router>
        <Route rule='/' reditect={SweetHome}>
            <SweetHome />
        </Route>
        <Route rule='/info' redirect={Info}>
            <Info />
        </Route>
    </Router>
    HTML);
}
