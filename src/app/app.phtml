<?php

namespace Fun;

function App()
{
    return (<<<HTML
    <Router>
        <Route method="GET" rule='^/$' redirect="Home" />
        <Route method="GET" rule='^/info(/)?$' redirect="Info" />
        <Route method="GET" rule='^/child(/)?$' redirect="Child" />
    </Router>
    HTML);
}
