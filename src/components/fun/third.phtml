<?php

namespace Fun;

use FunCom\Template\TemplateEngine;

use function FunCom\Hooks\useEffect;

function Third()
{
    $code = '';
    useEffect(function() use (&$code) {
        $engine = new TemplateEngine('Child');
        $dictionary = ["firstname" => "David"];
        $code = $engine->render($dictionary);
    });

    return (<<<HTML
        {{ code }}
    HTML);
}