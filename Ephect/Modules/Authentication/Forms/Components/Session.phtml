<?php

namespace Ephect\Modules\Authentication\Forms\Components;

function Session(object $props): string
{
    return (<<< HTML
    <input type="hidden" name="SESSION-ID" value="{{ props->sessionId }}" >
    HTML);
}
