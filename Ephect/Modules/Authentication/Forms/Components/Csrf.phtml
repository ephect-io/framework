<?php

namespace Ephect\Modules\Authentication\Forms\Components;

function Csrf(object $props): string
{
    $csrfFieldName = $props->name;
    $csrfToken = $props->token;

    return (<<< HTML
    <input type="hidden" name="{{ csrfFieldName }}" value="{{ csrfToken }}">
    HTML);
}