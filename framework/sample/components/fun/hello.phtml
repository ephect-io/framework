<?php

namespace Fun;

use function Ephect\Hooks\useEffect;
use function Ephect\Hooks\useQueryArgument;

function Hello() {

    useEffect(function() use (/* string */ &$name) {
        $name = useQueryArgument('name');
    });
    
    return (<<< HTML
        Hello {{ name }}
    HTML);
}
